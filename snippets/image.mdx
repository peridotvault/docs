export const Image = ({ src, alt }) => (
  <img
    src={src}
    style={{
      borderRadius: 30,
      maxHeight: 500,
      objectFit: "cover",
      width: "100%",
      objectPosition: "top",
    }}
    alt={alt}
  />
);
